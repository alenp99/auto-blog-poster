{
  "name": "auto-blog-poster",
  "version": "1.0.0",
  "description": "Automated blog generation and publishing system using Gemini AI + n8n",
  "private": true,
  "scripts": {
    "generate": "ts-node scripts/generate_and_publish_blog.ts",
    "log": "ts-node scripts/save_run_log.ts",
    "build": "npx tsc",
    "run:all": "npm run generate && npm run log",
    "start": "cd dashboard && npm install && node index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
